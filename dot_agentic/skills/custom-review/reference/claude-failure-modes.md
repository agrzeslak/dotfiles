# Moved

The failure modes are now the **Failure signature** lines under each of the five root review
primitives in `review-primitives.md` (a failure mode is what skipping a primitive looks like).

This stub can be deleted.
